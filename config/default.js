const {
  version,
  description
} = require('../package.json')

module.exports = {
  version,
  description,
  gitUser: 'sprucelabsai',
  repositories: {
    web: 'com-sprucebot-hello',
    api: 'com-sprucebot-api'
  }
}
