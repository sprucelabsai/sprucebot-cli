const {
  version,
  description
} = require('../package.json')

module.exports = {
  version,
  description,
  gitUser: 'sprucelabsai',
  repositories: {
    web: 'com-sprucebot-web',
    api: 'com-sprucebot-api'
  }
}
